[project]
name = "dualcast"
version = "0.1.0"
description = ""
authors = [
    {name = "KKKTa",email = "u044364j@ecs.osaka-u.ac.jp"}
]
readme = "README.md"
requires-python = "==3.11.9"
package-mode = false
dependencies = [
    "pandas (>=3.0.3,<4.0.0)",
    "scikit-learn (>=1.8.0,<2.0.0)",
    "seaborn (>=0.13.2,<0.14.0)",
    "lmfit (>=1.3.4,<2.0.0)",
    "numba (>=0.65.1,<0.66.0)",
    "statsmodels (>=0.14.6,<0.15.0)",
    "tqdm (>=4.67.3,<5.0.0)",
    "tensorly (>=0.9.0,<0.10.0)",
    "frouros (>=0.9.0,<0.10.0)",
    "itertools-len (>=1.3,<2.0)",
    "ipynb (>=0.5.1,<0.6.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false 
[tool.poetry.group.dev.dependencies]
ipykernel = "^7.2.0"

